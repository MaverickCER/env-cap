---
"@maverickcer/env-cap": minor
---

Add the Change Model: `buildChangeModel()`/`ChangeModel` wrap the existing `ManifestChangeReport` (unchanged) in a versioned, model-namespaced shape, giving it the same standing as env-cap's other canonical fact models. See [ADR 0030](specs/decisions/0030-change-model-wraps-manifest-change-report.md).
