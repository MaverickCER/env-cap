---
"@maverickcer/env-cap": minor
---

Exports `VariableEvidenceDocs` (previously an unexported internal
interface) from the package root, alongside the existing `ContractDocs`/
`VariableDocs` type exports.
