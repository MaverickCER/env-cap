# Fake but plausible values for local development and CI -- intentionally
# committed (see .gitignore's comment) so this example's runtime behavior is
# actually exercised. None of these are real credentials.
#
# STRIPE_KEY is deliberately absent, not just blank -- that's the entire
# point of this example. See README.md.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
