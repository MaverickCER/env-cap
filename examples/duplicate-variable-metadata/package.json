{
  "name": "env-cap-example-duplicate-variable-metadata",
  "private": true,
  "type": "module",
  "description": "Two unrelated, both-active features document the same variable name differently -- generation succeeds, with a duplicate-variable-documentation warning, unless escalated via ENV_CAP_STRICT.",
  "scripts": {
    "generate:env": "node scripts/generate-manifest.mjs",
    "generate:env:strict": "ENV_CAP_STRICT=1 node scripts/generate-manifest.mjs",
    "typecheck": "npm run generate:env && tsc --noEmit",
    "start": "npm run generate:env && tsx --env-file=.env src/app.ts"
  },
  "dependencies": {
    "@maverickcer/env-cap": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
