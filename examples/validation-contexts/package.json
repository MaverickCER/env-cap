{
  "name": "env-cap-example-validation-contexts",
  "private": true,
  "type": "module",
  "description": "One manifest, one schema, two entry points: a server process and a client process each validate the same manifest with different activeContexts, and each demonstrates both reading its own variable and being refused a variable that belongs to the other context.",
  "scripts": {
    "generate:env": "node scripts/generate-manifest.mjs",
    "typecheck": "npm run generate:env && tsc --noEmit",
    "example:server": "npm run generate:env && tsx --env-file=.env src/server.ts",
    "example:client": "npm run generate:env && tsx --env-file=.env src/client.ts",
    "start": "npm run example:server"
  },
  "dependencies": {
    "@maverickcer/env-cap": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
