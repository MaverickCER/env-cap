{
  "name": "env-cap-example-composable-boilerplates",
  "private": true,
  "type": "module",
  "description": "Three interchangeable database capabilities (postgres, mongodb, prisma) each own their own env.schema.ts -- no central schema to edit when a capability is added, removed, or swapped.",
  "scripts": {
    "generate:env": "node scripts/generate-manifest.mjs",
    "typecheck": "npm run generate:env && tsc --noEmit",
    "start": "npm run generate:env && tsx --env-file=.env src/app.ts"
  },
  "dependencies": {
    "@maverickcer/env-cap": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}